/**
 * SaaS marketing shell: ~1240px max width (common 1200–1320px range), responsive gutters.
 * Slightly tighter horizontal padding on large screens so content uses width without feeling boxed.
 */
export const pageShellClass =
  'mx-auto w-full max-w-[min(100%,1240px)] px-3 sm:px-4 md:px-5 lg:px-6 xl:px-7'

/** Long-form / legal: comfortable reading width */
export const pageShellNarrowClass =
  'mx-auto w-full max-w-[min(100%,42rem)] px-3 sm:px-4 md:px-5'
