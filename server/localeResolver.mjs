/**
 * @deprecated Import from localeHelpers.mjs — kept for backward compatibility.
 */
export {
  RESOLVED_FROM,
  findLocaleRecord,
  resolveLocaleRecord,
  mergePayloadFromBaseline,
  resolveContent,
  resolveContentWithBaseline,
  shapePublicLocaleView,
  parseSoftwareLocalePath,
} from './localeHelpers.mjs'
